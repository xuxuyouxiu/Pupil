# Pupil 路线图 v1.0 —— 2026-08-27 规划（四批递进）

> 状态：执行中 | 基线版本：v0.8.4 | 本文是批次内所有工作的排序依据
> 原则：每批独立可发布（打 tag 即出正式版），批内条目按表序逐个完成

---

## 批次一：小而关键（当天下一版 v0.9.0）

| # | 条目 | 验收标准 | 状态 |
|---|------|---------|------|
| 1.1 | **静默装完自启修复** | 安装批处理显式 `start Pupil.exe`；热更后球必须自动回来（不再需要手动开） | ☑ |
| 1.2 | **勿扰定时时长** | 右键菜单 + 设置各加：30 分钟 / 1 小时 / 2 小时 / 到明早 9 点；倒计时显示在面板顶栏与托盘提示；到期自动解除并广播 | ☑ |
| 1.3 | **单会话静音** | 会话行右键「忽略此会话通知」；持久化（key 列表）；被忽略会话正常显示状态但不发声不弹 Toast，行上有小静音图标可随时恢复 | ☑ |
| 1.4 | **config 原子写 + 拖球节流** | config.json 改 tmp+rename 原子写；ballPosition 保存节流 ≥500ms；拖动结束立即落盘 | ☑ |

## 批次二：核心卖点（v0.10.0）

| # | 条目 | 验收标准 | 状态 |
|---|------|---------|------|
| 2.1 | **waiting_input 全源覆盖** | codex（rollout 审批/输入请求事件，真实信号）；dsh（宿主 status 容错映射 wait/input/approv/confirm/permission，边沿触发+恢复信号）；claude 原有；hermes/zcode 为轮次型源——「等待用户」与「回合完成」语义同义（done 提醒已覆盖），不引入误报启发式（决策记录于此） | ☑ |
| 2.2 | **球体多会话徽标** | ≥2 活跃会话时球左上角叠数字徽标（>9 显示 9+），颜色随聚合状态着色；单会话不显示 | ☑ |
| 2.3 | **托盘状态色 + 悬停摘要** | 托盘图标按最高优先级状态着色（勿扰时灰色）；hover 文案「2 运行 · 1 等待 · 1 完成」；订阅快照自动刷新（签名去重） | ☑ |

## 批次三：数据智能（v0.11.0）

| # | 条目 | 验收标准 | 状态 |
|---|------|---------|------|
| 3.1 | **tokens/cost 采集** | claude-code 日志 `message.usage`（含缓存读/写）接入，payload.usage 随事件流；SessionRegistry 按会话累计（turn_started 重置本轮窗口）；定价注入式（config.pricing 覆盖，内置 claude-code 系 $3/$15 每百万）。**hermes messages 表无 tokens 列，标记不适用**；capability 'tokens' 实质达成 | ☑ |
| 3.2 | **面板用量展示** | 会话行时长后追加「· 12.3k · $0.08」（会话累计；成本为 0 时隐藏价格） | ☑ |
| 3.3 | **每日简报** | core/digest.ts 纯类（注入时钟，6 个单测）：完成/出错/运行时长/token 消耗按日累计，默认 21:00（config.digestHour）首过 tick 触发，空日不报；勿扰时静默 | ☑ |

## 批次四：规模化（v1.0.0）

| # | 条目 | 验收标准 | 状态 |
|---|------|---------|------|
| 4.1 | **差量更新** | 更新内核迁移 electron-updater（阿里云 CDN generic feed 优先 + GitHub provider 兜底），blockmap 差量下载；保留代理预检/直连兜底/取证日志/强杀安装批处理；完整 sha512 内建 | ☑ |
| 4.2 | **i18n 英文界面** | shared/i18n.ts zh/en 字典（~120 键）；跟随系统语言；面板/会话行/事件历史/设置全量 + 托盘菜单/通知/简报（正文符号化语言无关）；README 补 EN 简介 | ☑ |
| 4.3 | **适配器 SDK 文档** | docs/adapter-sdk.md：事件契约、三通道（HTTP/Hooks/external）、工具函数索引、贡献检查单 | ☑ |

---

## 明确不做（本期）

- 会话详情大视图（信息密度收益低、面板形态约束大）
- macOS/Linux 移植（win32 依赖是核心链路，成本远超收益）
- 跨设备同步/云（本地优先是产品红线）

## 风险与依赖

- 1.1 需真机验证 NSIS /S + runAfterFinish 实际行为（文档结论不可靠）
- 2.1 各源推断逻辑需真实会话数据回归（本机有 claude/codex/hermes/zcode 四源可用）
- 4.1 electron-updater 与自研镜像链的兼容策略：CDN feed 放 latest.yml，GitHub 作 fallback provider
